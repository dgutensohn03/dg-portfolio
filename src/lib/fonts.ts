import { Inter } from "next/font/google";

export const GeistSans = Inter({ subsets: ["latin"], variable: "--geist-sans" });
export const GeistMono = Inter({ subsets: ["latin"], variable: "--geist-mono" });
