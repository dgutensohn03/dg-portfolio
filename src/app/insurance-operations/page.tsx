"use client";

import Link from "next/link";
import { ArrowLeft, ArrowRight, Database, ExternalLink, Github, Layers3, RefreshCw, ShieldCheck, TestTube2, Workflow } from "lucide-react";

const live = "https://dgutensohn03.github.io/dotnet-insurance-dashboard";
const chapters = ["Product", "Architecture", "Claim flow", "Lifecycle", "UX", "Quality", "Delivery", "Decisions"];

function Label({ n, children }: { n: string; children: React.ReactNode }) {
  return <p className="flex flex-wrap items-center gap-2 text-[10px] font-bold uppercase tracking-[.16em] text-[var(--accent)] sm:gap-3 sm:text-[11px] sm:tracking-[.2em]"><span className="font-mono text-sm sm:text-base">{n}</span><span className="h-px w-6 bg-[var(--accent)] sm:w-8" />{children}</p>;
}
function Section({ id, n, title, children, tint=false }: { id:string;n:string;title:string;children:React.ReactNode;tint?:boolean }) {
  return <section id={id} className={`${tint?"case-band border-y border-[var(--hairline)]":"case-base"} scroll-mt-28 px-4 py-16 sm:px-6 sm:py-24 lg:py-28`}><div className="mx-auto max-w-7xl"><Label n={n}>{title}</Label>{children}</div></section>;
}
function Card({ title, children }: { title:string;children:React.ReactNode }) {
  return <div className="min-w-0 rounded-2xl border border-[var(--hairline)] bg-[var(--case-card)] p-5 sm:p-6"><p className="text-[10px] font-bold uppercase tracking-[.16em] text-[var(--accent)]">{title}</p><div className="mt-3 break-words text-sm leading-7 text-[var(--muted)]">{children}</div></div>;
}
function Code({ title, children }: { title:string;children:string }) {
  return <div className="min-w-0 overflow-hidden rounded-2xl border border-white/10 bg-[#09111f] shadow-xl"><div className="flex min-w-0 items-center justify-between gap-3 border-b border-white/10 px-4 py-3"><div className="flex shrink-0 gap-1.5"><i className="h-2.5 w-2.5 rounded-full bg-white/20"/><i className="h-2.5 w-2.5 rounded-full bg-white/20"/><i className="h-2.5 w-2.5 rounded-full bg-white/20"/></div><span className="min-w-0 truncate font-mono text-[10px] text-slate-500">{title}</span></div><pre className="max-w-full overflow-x-auto p-4 text-[11px] leading-6 text-slate-300 sm:p-5 sm:text-[12px] sm:leading-7"><code>{children}</code></pre></div>;
}
function Preview({ title, path="" }: { title:string;path?:string }) {
  return <figure className="min-w-0"><div className="overflow-hidden rounded-[18px] border border-[var(--hairline)] bg-[var(--case-card)] shadow-2xl shadow-black/10 sm:rounded-[22px]"><div className="flex min-w-0 items-center gap-2 border-b border-[var(--hairline)] px-3 py-3 sm:px-4"><div className="flex shrink-0 gap-1.5"><span className="h-2.5 w-2.5 rounded-full bg-[var(--hairline)]"/><span className="h-2.5 w-2.5 rounded-full bg-[var(--hairline)]"/><span className="h-2.5 w-2.5 rounded-full bg-[var(--hairline)]"/></div><span className="min-w-0 truncate text-[9px] font-semibold uppercase tracking-[.12em] text-[var(--muted)] sm:ml-2 sm:text-[10px] sm:tracking-[.14em]">InsureOps · {title}</span></div><div className="relative h-[520px] w-full bg-white sm:h-[560px]"><iframe title={`InsureOps ${title} live preview`} src={`${live}${path}`} loading="lazy" className="h-full w-full border-0"/></div></div><figcaption className="mt-3 flex flex-col gap-2 text-xs text-[var(--muted)] sm:flex-row sm:items-center sm:justify-between"><span>Interactive preview with fictional demo data.</span><a href={`${live}${path}`} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 font-semibold text-[var(--accent)]">Open dashboard <ExternalLink size={12}/></a></figcaption></figure>;
}

export default function InsuranceOperationsCaseStudy(){return <div className="case-study min-h-screen overflow-x-hidden text-[var(--fg)]">
<header className="sticky top-0 z-50 px-3 py-3 sm:px-6"><nav className="glass mx-auto flex max-w-7xl items-center justify-between gap-3 rounded-2xl px-4 py-3"><Link href="/" className="inline-flex shrink-0 items-center gap-2 text-sm text-[var(--muted)] hover:text-[var(--accent)]"><ArrowLeft size={16}/>Portfolio</Link><span className="hidden text-[10px] font-bold tracking-[.22em] text-[var(--muted)] sm:block">DG · DO GOOD</span><a href="https://github.com/dgutensohn03/dotnet-insurance-dashboard" target="_blank" rel="noreferrer" aria-label="View InsureOps source on GitHub" className="shrink-0 text-[var(--muted)] hover:text-[var(--accent)]"><Github size={18}/></a></nav></header>
<main>
<section className="case-base px-4 pb-16 pt-12 sm:px-6 sm:pb-24 sm:pt-24"><div className="mx-auto max-w-7xl"><Label n="00">Full-stack engineering case study</Label><h1 className="mt-6 max-w-6xl text-[clamp(2.7rem,12vw,5.75rem)] font-semibold leading-[.96] tracking-[-.05em]">Insurance operations,<span className="block text-[var(--accent)]">designed like a real product.</span></h1><p className="mt-7 max-w-3xl text-base leading-8 text-[var(--muted)] sm:text-xl">InsureOps is a fictional insurance operations platform I built with C#/.NET 8, Blazor WebAssembly, and ASP.NET Core. I used it to explore a practical question: how should a data-heavy internal product behave when operators need speed, context, and recoverable actions?</p><div className="mt-8 flex flex-wrap gap-3"><a className="btn-solid" href={live} target="_blank" rel="noreferrer">Explore InsureOps <ExternalLink size={15}/></a><a className="btn" href="#s1">Read the story <ArrowRight size={16}/></a></div><div className="mt-10 grid gap-px overflow-hidden rounded-2xl border border-[var(--hairline)] bg-[var(--hairline)] sm:grid-cols-2 lg:grid-cols-4">{[["STACK","C# · .NET 8 · Blazor · ASP.NET Core"],["WORKFLOWS","Policies · claims · customers · analytics"],["QUALITY","xUnit · CI deployment gate"],["DOMAIN","Recoverable archive / restore lifecycle"]].map(([h,t])=><div key={h} className="min-w-0 bg-[var(--case-card)] p-5 sm:p-6"><p className="text-xs font-bold text-[var(--accent)]">{h}</p><p className="mt-2 break-words text-sm leading-6 text-[var(--muted)]">{t}</p></div>)}</div></div></section>
<div className="sticky top-[76px] z-40 hidden border-y border-[var(--hairline)] bg-[var(--case-base)]/95 backdrop-blur-xl lg:block"><nav className="mx-auto flex max-w-7xl gap-6 overflow-x-auto px-6 py-3">{chapters.map((c,i)=><a key={c} href={`#s${i+1}`} className="whitespace-nowrap text-[10px] font-semibold text-[var(--muted)] hover:text-[var(--accent)]">{String(i+1).padStart(2,"0")} {c}</a>)}</nav></div>

<Section id="s1" n="01" title="The product" tint><div className="mt-8 grid min-w-0 gap-10 lg:grid-cols-[.72fr_1.28fr] lg:items-start"><div className="min-w-0"><h2 className="text-3xl font-semibold sm:text-5xl">Start with the operator, not the framework.</h2><p className="mt-5 leading-7 text-[var(--muted)]">The dashboard is organized around the questions an operations user needs answered quickly: What is happening across the portfolio? Which claims need attention? What record am I looking at? What action can I safely take next?</p><div className="mt-7 grid gap-3"><Card title="Design direction">Dense tables support scanning. Detail drawers preserve list context. Focused forms open in modals instead of sending the operator to another page.</Card><Card title="Demo environment">The public site uses deterministic fictional data in the browser so the full product can run on GitHub Pages. Changes in the demo reset when the page reloads.</Card></div></div><Preview title="Operations overview"/></div></Section>

<Section id="s2" n="02" title="Architecture"><div className="mt-8"><h2 className="max-w-4xl text-3xl font-semibold sm:text-5xl">One UI, two ways to reach data.</h2><p className="mt-5 max-w-3xl leading-7 text-[var(--muted)]">Blazor pages depend on <code>IInsuranceDataService</code> instead of a concrete data source. The public demo uses an in-browser implementation; the full-stack path uses an HTTP implementation backed by ASP.NET Core.</p><div className="mt-9 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[[Layers3,"Blazor UI","Pages, forms, filters, drawers and modals"],[Workflow,"Service contract","A typed boundary for data operations"],[RefreshCw,"ASP.NET Core","HTTP routes and lifecycle commands"],[Database,"Repository","A replaceable persistence boundary"]].map(([Icon,h,t])=>{const I=Icon as typeof Layers3;return <div key={String(h)} className="min-w-0 rounded-2xl border border-[var(--accent)]/25 bg-[var(--accent)]/[.045] p-5"><I className="text-[var(--accent)]" size={22}/><h3 className="mt-5 font-semibold">{String(h)}</h3><p className="mt-2 text-xs leading-5 text-[var(--muted)]">{String(t)}</p></div>})}</div><div className="mt-8 grid min-w-0 gap-4 lg:grid-cols-2"><Code title="IInsuranceDataService.cs">{`Task<IReadOnlyList<Claim>> GetClaimsAsync();\nTask<Claim> AddClaimAsync(Claim claim);\nTask<Claim> ArchiveClaimAsync(int id);\nTask<Claim> RestoreClaimAsync(int id);\n\nTask<Customer> UpdateCustomerAsync(Customer customer);\nTask<Customer> ArchiveCustomerAsync(int id);\nTask<Customer> RestoreCustomerAsync(int id);`}</Code><Card title="Why the boundary matters">Presentation code does not change when the data source changes. That makes the static portfolio build possible without coupling the UI to demo-only behavior.</Card></div></div></Section>

<Section id="s3" n="03" title="Follow one claim" tint><div className="mt-8 grid min-w-0 gap-10 lg:grid-cols-[.7fr_1.3fr] lg:items-start"><div className="min-w-0"><h2 className="text-3xl font-semibold sm:text-5xl">One record connects the layers.</h2><p className="mt-5 leading-7 text-[var(--muted)]">The demo keeps a stable example, <strong>CLM-10482</strong> on policy <strong>POL-48392</strong>, with a $42,850 exposure. Following that record makes the architecture easier to understand than discussing each technology in isolation.</p><div className="mt-8 space-y-4">{[["01","Repository","Provides the typed claim"],["02","API","Exposes it over HTTP in full-stack mode"],["03","Client service","Returns the data to the Blazor page"],["04","LINQ","Filters and prioritizes operational records"],["05","Blazor","Renders the table; a click opens its detail drawer"]].map(([n,h,t])=><div key={n} className="flex min-w-0 gap-4 border-b border-[var(--hairline)] pb-4"><span className="shrink-0 font-mono text-xs text-[var(--accent)]">{n}</span><div className="min-w-0"><strong className="text-sm">{h}</strong><p className="mt-1 break-words text-sm text-[var(--muted)]">{t}</p></div></div>)}</div></div><Preview title="Claims operations" path="/claims"/></div></Section>

<Section id="s4" n="04" title="A safer record lifecycle"><div className="mt-8 grid min-w-0 gap-10 lg:grid-cols-2"><div className="min-w-0"><h2 className="text-3xl font-semibold sm:text-5xl">I replaced destructive deletion with recovery.</h2><p className="mt-5 leading-7 text-[var(--muted)]">Deleting a claim or customer record outright was easy to build, but it did not fit the product. I changed the workflow to archive and restore records instead. Archived records leave active operations and calculations while remaining available through lifecycle filters.</p><div className="mt-8 grid gap-3 sm:grid-cols-3">{[["ACTIVE","Included in normal work"],["ARCHIVED","Retained outside active work"],["RESTORED","Returned to active workflows"]].map(([h,t])=><Card key={h} title={h}>{t}</Card>)}</div></div><div className="min-w-0"><Code title="Claim.cs">{`public bool IsArchived { get; set; }\npublic DateTimeOffset? ArchivedAt { get; set; }\npublic DateTimeOffset LastUpdatedAt { get; set; }\n    = DateTimeOffset.UtcNow;`}</Code><div className="mt-4"><Card title="Why this matters">The lifecycle decision affects the model, service contract, API, repository, filters, KPIs, analytics and tests. It became an architectural decision rather than a button-label change.</Card></div></div></div></Section>

<Section id="s5" n="05" title="Interaction design" tint><div className="mt-8"><h2 className="max-w-4xl text-3xl font-semibold sm:text-5xl">Keep context when context matters.</h2><div className="mt-8 grid gap-3 md:grid-cols-3"><Card title="Create / edit">Focused forms use modals with validation, saving states and a clear commit-or-cancel decision.</Card><Card title="Inspect">Customer and claim details use drawers so the table remains part of the operator's context.</Card><Card title="Archive / restore">Lifecycle actions explain what will happen and remain reversible.</Card></div><div className="mt-10 grid min-w-0 gap-8 lg:grid-cols-2"><Preview title="Customers" path="/customers"/><div className="grid content-start gap-3"><Card title="Responsive behavior">Controls collapse to a single column on small screens. Drawers become bottom-sheet-style surfaces, modals stay inside the viewport, and wide operational tables scroll inside their own panel rather than widening the page.</Card><Card title="Accessibility">Dialogs use dialog semantics and accessible labels, route changes move assistive focus to the new page heading, and interactive controls retain visible focus treatment.</Card><Card title="Restraint">Search, filters and navigation stay inline. A modal is used only when the task benefits from temporary focus.</Card></div></div></div></Section>

<Section id="s6" n="06" title="Quality"><div className="mt-8 grid min-w-0 gap-10 lg:grid-cols-[.75fr_1.25fr]"><div className="min-w-0"><TestTube2 size={30} className="text-[var(--accent)]"/><h2 className="mt-5 text-3xl font-semibold sm:text-5xl">Test the behavior that carries risk.</h2><p className="mt-5 leading-7 text-[var(--muted)]">The xUnit suite checks the stable demo record, calculated summary behavior, customer workflows, and recoverable claim lifecycle. Those tests run before the public build is published.</p></div><div className="min-w-0"><Code title="DemoInsuranceDataServiceTests.cs">{`[Fact]\npublic async Task Claim_archive_is_recoverable()\n{\n    var service = new DemoInsuranceDataService();\n    var claim = (await service.GetClaimsAsync())\n        .Single(c => c.ClaimNumber == "CLM-10482");\n\n    await service.ArchiveClaimAsync(claim.Id);\n    Assert.True(claim.IsArchived);\n\n    await service.RestoreClaimAsync(claim.Id);\n    Assert.False(claim.IsArchived);\n}`}</Code><div className="mt-4"><Card title="Where I would take quality next">Component tests for modal and drawer behavior, API integration tests, Playwright critical paths, and automated accessibility checks would be the next additions for a production team.</Card></div></div></div></Section>

<Section id="s7" n="07" title="Delivery" tint><div className="mt-8 grid min-w-0 gap-10 lg:grid-cols-2"><div className="min-w-0"><ShieldCheck size={30} className="text-[var(--accent)]"/><h2 className="mt-5 text-3xl font-semibold sm:text-5xl">Tests gate the public build.</h2><p className="mt-5 leading-7 text-[var(--muted)]">GitHub Actions runs the test project before publishing the Blazor client. A compile failure in the test project once stopped deployment, leaving the last successful build public until the problem was fixed.</p><div className="mt-7"><Card title="Static hosting">GitHub Pages serves the WebAssembly client, so the public build selects the demo service. Local full-stack development can use the ASP.NET Core API path.</Card></div></div><Code title="deploy-pages.yml">{`- name: Run unit tests\n  run: dotnet test InsuranceDashboard.Tests/InsuranceDashboard.Tests.csproj -c Release\n\n- name: Publish Blazor client\n  run: dotnet publish InsuranceDashboard.Client/InsuranceDashboard.Client.csproj -c Release -o release`}</Code></div></Section>

<Section id="s8" n="08" title="Engineering decisions"><div className="mt-8"><h2 className="max-w-4xl text-3xl font-semibold sm:text-5xl">The choices I would revisit as the system grows.</h2><p className="mt-5 max-w-3xl leading-7 text-[var(--muted)]">A portfolio demo and a production insurance platform have different constraints. These are the boundaries I deliberately kept visible instead of hiding them behind polish.</p><div className="mt-8 grid gap-3 md:grid-cols-2 xl:grid-cols-3"><Card title="Persistence">The portable demo is in-memory. A deployed system would need durable transactional storage and migrations.</Card><Card title="Relationships">Policies still carry customer display names in demo data. Stable customer IDs and enforced relationship integrity would be the stronger schema.</Card><Card title="Loss ratio">The dashboard uses a simplified operational signal rather than an actuarial calculation of incurred loss against earned premium for a matched period.</Card><Card title="Security">A production deployment would add authentication, policy-based authorization, actor identity and durable audit events around sensitive actions.</Card><Card title="Repository layer">The boundary is useful here for demonstrating replaceable persistence. With EF Core, I would keep it only if it adds domain value beyond mirroring DbSet.</Card><Card title="Blazor WebAssembly">It provides typed .NET client development and component reuse; privileged operations still belong on the server and initial runtime size remains a tradeoff.</Card></div><div className="mt-12 rounded-3xl border border-[var(--hairline)] bg-[var(--case-card)] p-6 sm:p-8"><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--accent)]">What this project demonstrates</p><h3 className="mt-4 max-w-4xl text-2xl font-semibold sm:text-3xl">I can move from an operator problem to UI behavior, typed application boundaries, domain rules, tests and deployment—and explain why each decision exists.</h3><div className="mt-7 flex flex-wrap gap-3"><a className="btn-solid" href={live} target="_blank" rel="noreferrer">Explore InsureOps <ExternalLink size={15}/></a><a className="btn" href="https://github.com/dgutensohn03/dotnet-insurance-dashboard" target="_blank" rel="noreferrer">View source <Github size={15}/></a></div></div></div></Section>
<section className="case-base px-4 py-16 sm:px-6 sm:py-20"><div className="mx-auto flex max-w-7xl flex-col gap-5 border-t border-[var(--hairline)] pt-10 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-semibold">InsureOps · fictional engineering case study</p><p className="mt-1 text-xs text-[var(--muted)]">Fictional demo data. Not affiliated with an insurer.</p></div><Link href="/" className="inline-flex items-center gap-2 text-sm font-semibold text-[var(--accent)]">Back to portfolio <ArrowRight size={15}/></Link></div></section>
</main></div>}
