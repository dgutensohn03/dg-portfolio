"use client";
import Link from "next/link";
import {ArrowLeft,ArrowRight,BookOpen,Check,Code2,Database,ExternalLink,Github,Layers3,Network,Server,ShieldCheck,Smartphone,TestTube2,Workflow,Zap} from "lucide-react";
import {useMemo,useState} from "react";

const claim={id:"CLM-10482",customer:"Jennifer Hart",policy:"POL-48392",amount:42850,status:"Investigating"};
const claims=[claim,{id:"CLM-10321",customer:"Michael Torres",policy:"POL-77102",amount:12300,status:"Open"},{id:"CLM-10294",customer:"Sarah Kim",policy:"POL-55192",amount:8450,status:"Pending"},{id:"CLM-10172",customer:"Robert Fields",policy:"POL-77102",amount:28900,status:"Investigating"},{id:"CLM-09943",customer:"Maria Lopez",policy:"POL-55218",amount:26750,status:"Pending"}];
const chapters=["Start","System","C# + .NET","Blazor","One claim","API","DI","Data","Security","Reliability","Delivery","Beyond web","Tradeoffs","Interview"];
function Label({n,children}:{n:string;children:React.ReactNode}){return <p className="flex items-center gap-3 text-[11px] font-bold uppercase tracking-[.2em] text-[var(--accent)]"><span className="font-mono text-base">{n}</span><span className="h-px w-8 bg-[var(--accent)]"/>{children}</p>}
function Code({title,children}:{title:string;children:React.ReactNode}){return <div className="overflow-hidden rounded-2xl border border-white/10 bg-[#09111f] shadow-2xl"><div className="flex items-center justify-between border-b border-white/10 px-4 py-3"><div className="flex gap-1.5"><i className="h-2.5 w-2.5 rounded-full bg-white/20"/><i className="h-2.5 w-2.5 rounded-full bg-white/20"/><i className="h-2.5 w-2.5 rounded-full bg-white/20"/></div><span className="font-mono text-[10px] text-slate-500">{title}</span></div><pre className="overflow-x-auto p-5 text-[12px] leading-7 text-slate-300"><code>{children}</code></pre></div>}
function Explain({title="Why it matters",children}:{title?:string;children:React.ReactNode}){return <div className="rounded-2xl border border-[var(--hairline)] bg-[var(--bg)]/65 p-5"><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[var(--accent)]">{title}</p><div className="mt-3 text-sm leading-7 text-[var(--muted)]">{children}</div></div>}
function Talk({children}:{children:React.ReactNode}){return <div className="rounded-2xl border border-[var(--accent)]/30 bg-[var(--accent)]/[.07] p-5"><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[var(--accent)]">How I’d explain it</p><p className="mt-3 text-sm leading-7 text-[var(--muted)]">{children}</p></div>}
function Term({term,children}:{term:string;children:React.ReactNode}){return <div className="border-l-2 border-[var(--accent)]/50 pl-4"><p className="font-mono text-xs font-semibold text-[var(--fg)]">{term}</p><p className="mt-1 text-xs leading-5 text-[var(--muted)]">{children}</p></div>}
function Section({id,n,title,children,tint=false}:{id:string;n:string;title:string;children:React.ReactNode;tint?:boolean}){return <section id={id} className={`${tint?"border-y border-[var(--hairline)] bg-[var(--bg)]/35":""} scroll-mt-32 px-5 py-16 sm:px-6 lg:py-28`}><div className="mx-auto max-w-7xl"><Label n={n}>{title}</Label>{children}</div></section>}
export default function Page(){const[minimum,setMinimum]=useState(25000);const results=useMemo(()=>claims.filter(c=>c.status!=="Closed"&&c.amount>=minimum).sort((a,b)=>b.amount-a.amount).slice(0,5),[minimum]);return <div className="min-h-screen text-[var(--fg)]">
<header className="sticky top-0 z-50 px-3 py-3 sm:px-6"><nav className="glass mx-auto flex max-w-7xl items-center justify-between rounded-2xl px-4 py-3"><Link href="/" className="inline-flex items-center gap-2 text-sm text-[var(--muted)] hover:text-[var(--accent)]"><ArrowLeft size={16}/>Portfolio</Link><span className="text-[10px] font-bold tracking-[.22em] text-[var(--muted)]">DG · DO GOOD</span><a href="https://github.com/dgutensohn03/dotnet-insurance-dashboard" target="_blank" rel="noreferrer" aria-label="View source" className="text-[var(--muted)] hover:text-[var(--accent)]"><Github size={18}/></a></nav></header>
<main>
<section className="px-5 pb-16 pt-14 sm:px-6 sm:pb-24 sm:pt-24"><div className="mx-auto max-w-7xl"><Label n="00">Engineering case study · guided walkthrough</Label><h1 className="mt-7 max-w-6xl text-5xl font-semibold leading-[.94] tracking-[-.055em] sm:text-7xl lg:text-[92px]">One dashboard.<span className="block text-[var(--accent)]">The whole engineering story.</span></h1><p className="mt-7 max-w-3xl text-lg leading-8 text-[var(--muted)] sm:text-xl">A progressive walkthrough of a full-stack insurance operations application. Start with what the system does, then follow one claim through C#, Blazor, HTTP, ASP.NET Core, dependency injection, and the data boundary—before examining security, reliability, delivery, and production tradeoffs.</p><div className="mt-9 flex flex-wrap gap-3"><a className="btn-solid" href="https://dgutensohn03.github.io/dotnet-insurance-dashboard/" target="_blank" rel="noreferrer">Open live dashboard<ExternalLink size={15}/></a><a className="btn" href="#s1">Begin the walkthrough<ArrowRight size={16}/></a></div><div className="mt-12 grid gap-px overflow-hidden rounded-2xl border border-[var(--hairline)] bg-[var(--hairline)] sm:grid-cols-3"><div className="bg-[var(--bg)] p-5"><p className="text-xs text-[var(--accent)]">FOR ANY READER</p><p className="mt-2 text-sm text-[var(--muted)]">Every chapter explains what a concept is before showing implementation.</p></div><div className="bg-[var(--bg)] p-5"><p className="text-xs text-[var(--accent)]">FOR ENGINEERS</p><p className="mt-2 text-sm text-[var(--muted)]">Boundaries, failure modes, testability, lifetimes, security, and tradeoffs are explicit.</p></div><div className="bg-[var(--bg)] p-5"><p className="text-xs text-[var(--accent)]">FOR THE INTERVIEW</p><p className="mt-2 text-sm text-[var(--muted)]">The same page doubles as a screen-sharing narrative and concept map.</p></div></div></div></section>
<div className="sticky top-[76px] z-40 hidden border-y border-[var(--hairline)] bg-[var(--bg)]/90 backdrop-blur-xl lg:block"><nav className="mx-auto flex max-w-7xl gap-6 overflow-x-auto px-6 py-3">{chapters.map((x,i)=><a key={x} href={`#s${i+1}`} className="whitespace-nowrap text-[10px] font-semibold text-[var(--muted)] hover:text-[var(--accent)]">{String(i+1).padStart(2,"0")} {x}</a>)}</nav></div>

<Section id="s1" n="01" title="Start here" tint><div className="mt-7 grid gap-10 lg:grid-cols-[.75fr_1.25fr]"><div><h2 className="text-3xl font-semibold sm:text-5xl">Start with the problem, not the framework.</h2><p className="mt-5 leading-7 text-[var(--muted)]">The application is a fictional internal insurance operations dashboard. Its job is to turn policy, customer, and claims data into an interface where an operator can understand portfolio health and act on claims that need attention.</p><Talk>I’d frame the project as a production-inspired system rather than a tutorial. The dashboard is the product. This case study explains the engineering underneath it.</Talk></div><div className="grid gap-px overflow-hidden rounded-2xl border border-[var(--hairline)] bg-[var(--hairline)] md:grid-cols-3">{[["Problem","Operational data loses value when users must hunt across disconnected screens."],["Solution","A responsive dashboard with clear domain models, reusable UI, an HTTP API, and explicit data boundaries."],["Goal","Demonstrate not just syntax, but how decisions connect from user interaction through deployment."]].map(([h,t])=><article key={h} className="bg-[var(--bg)] p-6"><p className="text-xs font-semibold text-[var(--accent)]">{h}</p><p className="mt-4 text-sm leading-7 text-[var(--muted)]">{t}</p></article>)}</div></div></Section>

<Section id="s2" n="02" title="Understand the system"><div className="mt-7"><h2 className="max-w-3xl text-3xl font-semibold sm:text-5xl">The simplest useful mental model.</h2><p className="mt-5 max-w-3xl leading-7 text-[var(--muted)]">Before naming frameworks, the system is just a person using a browser, an application asking a server for information, and a server reading data. Everything else exists to make those responsibilities maintainable.</p><div className="mt-9 grid gap-3 sm:grid-cols-4">{[["01","Person","Chooses an action"],["02","Browser","Renders the interface"],["03","Server","Processes the request"],["04","Data","Stores domain state"]].map(([n,h,t])=><div key={h} className="rounded-2xl border border-[var(--hairline)] p-5"><span className="font-mono text-xs text-[var(--accent)]">{n}</span><h3 className="mt-5 font-semibold">{h}</h3><p className="mt-2 text-xs text-[var(--muted)]">{t}</p></div>)}</div><div className="my-10 flex justify-center text-[var(--accent)]"><ArrowRight/></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[[Layers3,"Blazor WebAssembly","Presentation: pages, components, state"],[Workflow,"Service abstraction","Client data boundary"],[Server,"ASP.NET Core API","HTTP routes and server behavior"],[Database,"Repository","Persistence boundary"]].map(([I,h,t])=>{const Icon=I as typeof Layers3;return <div key={String(h)} className="rounded-2xl border border-[var(--accent)]/25 bg-[var(--accent)]/[.04] p-5"><Icon size={22} className="text-[var(--accent)]"/><h3 className="mt-5 font-semibold">{String(h)}</h3><p className="mt-2 text-xs leading-5 text-[var(--muted)]">{String(t)}</p></div>})}</div><div className="mt-6 grid gap-3 lg:grid-cols-2"><Explain title="Why boundaries exist">A boundary gives one part of the system a clear responsibility and prevents implementation details from leaking everywhere. The UI should not need to know how a database works; an API route should not need to know how a component renders.</Explain><Explain title="Senior-level nuance">More layers are not automatically better. Each abstraction has a maintenance cost. I keep a boundary when it isolates a meaningful responsibility, supports testing or replacement, or prevents coupling that would otherwise spread.</Explain></div></div></Section>

<Section id="s3" n="03" title="C# + .NET foundation" tint><div className="mt-7 grid gap-10 lg:grid-cols-[.75fr_1.25fr]"><div><h2 className="text-3xl font-semibold sm:text-5xl">Language, platform, frameworks.</h2><p className="mt-5 leading-7 text-[var(--muted)]">C# is the programming language. .NET is the runtime and platform. Blazor and ASP.NET Core are frameworks built on .NET for UI and web/server development.</p><div className="mt-7 space-y-4"><Term term="C#">Strongly typed language used throughout this project.</Term><Term term=".NET 8">Runtime, base class libraries, tooling, dependency injection infrastructure, serialization, and more.</Term><Term term="Blazor">Component framework used for the browser UI.</Term><Term term="ASP.NET Core">Framework used for HTTP APIs and server-side behavior.</Term></div></div><div><Code title="Claim.cs"><>{`public record Claim(\n    string Id,\n    string Customer,\n    string Policy,\n    decimal Amount,\n    string Status);`}</></Code><div className="mt-3 grid gap-3 sm:grid-cols-2"><Explain title="Why strong typing?">A <code>Claim</code> has a known shape. The compiler can catch incompatible values before runtime, tooling can autocomplete members, and contracts remain explicit across the codebase.</Explain><Explain title="Why decimal for money?">Binary floating-point types can represent many decimal fractions approximately. <code>decimal</code> is designed for base-10 precision and is the conventional choice for financial amounts.</Explain></div><div className="mt-3"><Explain title="Nullability + collections">Nullable reference types make the possibility of missing values visible in the type system. Generic collections such as <code>List&lt;Claim&gt;</code> preserve the type of every item instead of passing loosely structured data around.</Explain></div></div></div></Section>

<Section id="s4" n="04" title="Blazor UI"><div className="mt-7 grid gap-10 lg:grid-cols-2"><div><h2 className="text-3xl font-semibold sm:text-5xl">The browser UI is a tree of components.</h2><p className="mt-5 leading-7 text-[var(--muted)]">A page composes smaller Razor components. Data flows down through parameters. User intent flows back through typed callbacks. Component state determines what is rendered.</p><div className="mt-7 grid gap-4 sm:grid-cols-2"><Term term="@page">Associates a Razor component with a URL route.</Term><Term term="@code">Contains C# fields, state, lifecycle methods, and handlers.</Term><Term term="[Parameter]">Declares input owned by a parent and consumed by a child.</Term><Term term="EventCallback<T>">Sends a typed event from child to parent.</Term><Term term="@bind / @onclick">Connect browser interaction to component state and C# handlers.</Term><Term term="OnInitializedAsync">Lifecycle hook commonly used for initial async loading.</Term></div></div><div><Code title="Claims.razor"><>{`@foreach (var claim in claims)\n{\n  <ClaimCard Claim="claim"\n    Selected="selectedClaim?.Id == claim.Id"\n    OnSelected="SelectClaim" />\n}\n\n@code {\n  private Claim? selectedClaim;\n  private void SelectClaim(Claim claim)\n    => selectedClaim = claim;\n}`}</></Code><div className="mt-3"><Talk>The parent owns selection state. It passes the current state into each child. A child reports a click through EventCallback instead of reaching into its parent. That one-way relationship keeps ownership explicit and components easier to reuse.</Talk></div><div className="mt-3"><Explain title="Accessibility is part of the component contract">Reusable UI should preserve semantic elements, keyboard behavior, visible focus, meaningful labels, error announcements, responsive reflow, contrast, and appropriate loading/empty states—not treat accessibility as a final audit.</Explain></div></div></div></Section>

<Section id="s5" n="05" title="Follow one claim" tint><div className="mt-7"><div className="max-w-3xl"><h2 className="text-3xl font-semibold sm:text-5xl">Follow CLM-10482 through every layer.</h2><p className="mt-5 leading-7 text-[var(--muted)]">Using one record removes abstraction from the explanation. Jennifer Hart’s $42,850 claim is the same domain object as it moves from storage to the screen.</p></div><div className="mt-9 grid gap-2 sm:grid-cols-2 lg:grid-cols-7">{[["01","DATA","Claim record"],["02","REPOSITORY","GetAllAsync"],["03","API","GET /api/claims"],["04","JSON","serialized response"],["05","CLIENT","List<Claim>"],["06","LINQ","exposure filter"],["07","UI","ClaimCard"]].map(([n,h,t])=><div key={h} className="rounded-xl border border-[var(--hairline)] bg-[var(--bg)] p-4"><span className="font-mono text-xs text-[var(--accent)]">{n}</span><p className="mt-3 text-xs font-bold">{h}</p><p className="mt-1 text-[11px] leading-5 text-[var(--muted)]">{t}</p></div>)}</div><div className="mt-6 grid gap-4 lg:grid-cols-3"><Code title="C# object"><>{`new Claim(\n  "${claim.id}",\n  "${claim.customer}",\n  "${claim.policy}",\n  ${claim.amount}m,\n  "${claim.status}");`}</></Code><Code title="JSON over HTTP"><>{`{\n  "id": "${claim.id}",\n  "customer": "${claim.customer}",\n  "amount": ${claim.amount},\n  "status": "${claim.status}"\n}`}</></Code><div className="rounded-2xl border border-[var(--hairline)] bg-[var(--bg)] p-5"><p className="text-xs text-[var(--accent)]">RENDERED RESULT</p><p className="mt-6 font-mono text-xs">{claim.id}</p><p className="mt-2 text-xl font-semibold">{claim.customer}</p><p className="mt-5 text-3xl font-semibold">${claim.amount.toLocaleString()}</p><p className="mt-2 text-sm text-[var(--muted)]">{claim.status}</p></div></div></div></Section>

<Section id="s6" n="06" title="ASP.NET Core API"><div className="mt-7 grid gap-10 lg:grid-cols-2"><div><Code title="Program.cs"><>{`app.MapGet("/api/claims", async (\n  IClaimsRepository repository) =>\n{\n  var claims = await repository.GetAllAsync();\n  return Results.Ok(claims);\n});`}</></Code><div className="mt-3"><Code title="ApiInsuranceDataService.cs"><>{`public async Task<List<Claim>> GetClaimsAsync()\n{\n  return await _httpClient\n    .GetFromJsonAsync<List<Claim>>("api/claims")\n    ?? [];\n}`}</></Code></div></div><div><h2 className="text-3xl font-semibold sm:text-5xl">HTTP is the boundary between browser and server.</h2><p className="mt-5 leading-7 text-[var(--muted)]">The client sends a GET request. ASP.NET Core routing matches the URL. Dependency injection supplies the repository. The endpoint awaits I/O, returns an HTTP result, and JSON serialization turns C# objects into bytes the browser can receive.</p><div className="mt-7 space-y-4"><Term term="async">Marks a method that can asynchronously await work.</Term><Term term="Task<T>">Represents an operation that will eventually produce a typed result.</Term><Term term="await">Suspends this method while I/O is pending without synchronously blocking the executing thread.</Term><Term term="GetFromJsonAsync<T>">Sends the request and deserializes JSON into the requested C# type.</Term><Term term="?? []">Provides an empty collection if the result is null.</Term></div><div className="mt-6"><Explain title="Important nuance"><code>async</code> does not make the network faster. For I/O-bound work, its value is that execution resources do not need to sit blocked while the application waits for the external operation to complete.</Explain></div></div></div></Section>

<Section id="s7" n="07" title="Dependency injection" tint><div className="mt-7 grid gap-10 lg:grid-cols-[.8fr_1.2fr]"><div><h2 className="text-3xl font-semibold sm:text-5xl">Ask for capability, not construction details.</h2><p className="mt-5 leading-7 text-[var(--muted)]">Without DI, an endpoint could construct <code>ClaimsRepository</code> itself. That couples HTTP behavior to a concrete implementation. Instead, the endpoint requests an interface and the application composition root decides what implementation fulfills it.</p><Talk>The endpoint says “I need something that behaves like IClaimsRepository.” It does not say “new up this exact class.” ASP.NET Core resolves the registered implementation for the current scope.</Talk></div><div><Code title="Program.cs · composition root"><>{`builder.Services.AddScoped<\n  IClaimsRepository, ClaimsRepository>();`}</></Code><div className="mt-4 grid gap-3 sm:grid-cols-3"><Explain title="AddScoped">One instance per DI scope. In server-side ASP.NET Core that normally aligns with an HTTP request.</Explain><Explain title="IClaimsRepository">The contract consumers depend on.</Explain><Explain title="ClaimsRepository">The concrete implementation the container provides.</Explain></div><div className="mt-3 grid gap-3 sm:grid-cols-3"><Term term="Transient">A new instance each time the service is resolved.</Term><Term term="Scoped">One instance for a scope/request.</Term><Term term="Singleton">One instance for the application lifetime; shared state and thread safety require care.</Term></div><div className="mt-5"><Explain title="Tradeoff">An interface is not valuable merely because it is an interface. If it only mirrors a class and no useful boundary exists, it can become ceremony. Here it expresses the persistence boundary and supports alternate implementations/testing.</Explain></div></div></div></Section>

<Section id="s8" n="08" title="Data architecture"><div className="mt-7 grid gap-10 lg:grid-cols-2"><div><h2 className="text-3xl font-semibold sm:text-5xl">Demo data today. Persistent storage in production.</h2><p className="mt-5 leading-7 text-[var(--muted)]">GitHub Pages is static hosting, so the public portfolio version uses <code>DemoInsuranceDataService</code>. The intended server shape keeps the client behind the same interface and lets the API own persistence.</p><div className="mt-7 grid gap-3 sm:grid-cols-2"><Explain title="Portfolio deployment">Blazor → IInsuranceDataService → DemoInsuranceDataService</Explain><Explain title="Production direction">Blazor → HTTP → ASP.NET Core → repository/application layer → EF Core → SQL</Explain></div></div><div><div className="grid gap-3 sm:grid-cols-2">{[[Database,"EF Core","Object-relational mapping, queries, change tracking, migrations."],[Server,"SQL database","Durable relational storage, indexes, constraints, transactions."],[Workflow,"DTO boundary","Shape API contracts independently from persistence entities."],[Zap,"Query discipline","Project only needed columns, paginate large results, inspect generated SQL."]].map(([I,h,t])=>{const Icon=I as typeof Database;return <div key={String(h)} className="rounded-2xl border border-[var(--hairline)] p-5"><Icon className="text-[var(--accent)]"/><h3 className="mt-5 font-semibold">{String(h)}</h3><p className="mt-2 text-xs leading-5 text-[var(--muted)]">{String(t)}</p></div>})}</div><div className="mt-4"><Explain title="Repository tradeoff with EF Core">EF Core’s <code>DbContext</code> already implements unit-of-work/repository-like behavior. In a larger system I would justify an additional repository based on domain boundaries, query ownership, test strategy, or persistence isolation—not add one automatically.</Explain></div></div></div></Section>

<Section id="s9" n="09" title="Security" tint><div className="mt-7 grid gap-10 lg:grid-cols-[.7fr_1.3fr]"><div><h2 className="text-3xl font-semibold sm:text-5xl">Identity is not permission.</h2><p className="mt-5 leading-7 text-[var(--muted)]"><b>Authentication</b> establishes who the caller is. <b>Authorization</b> determines what that identity is allowed to do. An insurance system would enforce permissions at server boundaries, not merely hide UI controls.</p></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[["401","Unauthenticated","Establish identity"],["403","Forbidden","Identity lacks permission"],["400","Bad request","Input failed validation"],["404","Not found","Resource is unavailable"]].map(([n,h,t])=><div key={n} className="rounded-2xl border border-[var(--hairline)] p-5"><p className="font-mono text-2xl text-[var(--accent)]">{n}</p><p className="mt-4 font-semibold">{h}</p><p className="mt-2 text-xs text-[var(--muted)]">{t}</p></div>)}<div className="sm:col-span-2 lg:col-span-4"><Explain title="Production considerations">Use an identity provider/OIDC, validate tokens server-side, apply role/policy-based authorization to protected resources, avoid exposing secrets to WebAssembly, validate all untrusted input, use HTTPS, log security-relevant events, and apply least privilege.</Explain></div></div></div></Section>

<Section id="s10" n="10" title="Reliability + testing"><div className="mt-7 grid gap-10 lg:grid-cols-2"><div><h2 className="text-3xl font-semibold sm:text-5xl">Design the unhappy path too.</h2><div className="mt-7 space-y-3">{[["Loading","Give immediate feedback; avoid layout instability."],["Empty","Explain that no records match rather than showing a blank surface."],["Validation","Prevent invalid input and return useful field/domain errors."],["Failure","Present an accessible error state and a safe retry path."],["Cancellation","Cancel obsolete requests when navigation or intent changes."],["Observability","Correlate logs, metrics, traces, and request IDs across boundaries."]].map(([h,t])=><div key={h} className="rounded-xl border border-[var(--hairline)] p-4"><b className="text-sm">{h}</b><p className="mt-1 text-xs leading-5 text-[var(--muted)]">{t}</p></div>)}</div></div><div><div className="rounded-2xl border border-[var(--hairline)] bg-[#09111f] p-6 text-slate-200"><p className="font-mono text-xs text-slate-500">verification.strategy</p>{[["Unit","Business rules + pure transformations"],["Component","Rendering + interaction contracts"],["Integration","API + serialization + persistence boundary"],["End-to-end","Critical user workflows in a real browser"]].map(([h,t],i)=><div key={h} className="mt-5 flex gap-4"><span className="font-mono text-xs text-cyan-400">0{i+1}</span><div><p className="text-sm font-semibold">{h}</p><p className="mt-1 text-xs text-slate-400">{t}</p></div></div>)}</div><div className="mt-4"><Explain title="What not to do">A large test count is not the goal. Tests should protect behavior and boundaries that matter. Over-mocking implementation details can make tests brittle while providing little confidence.</Explain></div></div></div></Section>

<Section id="s11" n="11" title="Delivery" tint><div className="mt-7 grid gap-10 lg:grid-cols-[.75fr_1.25fr]"><div><h2 className="text-3xl font-semibold sm:text-5xl">Code is not shipped until the pipeline says it is.</h2><p className="mt-5 leading-7 text-[var(--muted)]">The portfolio uses GitHub Actions to publish the Blazor WebAssembly client to GitHub Pages. A production pipeline would separate build, verification, artifact creation, environment deployment, configuration, and rollback strategy.</p></div><div className="grid gap-2 sm:grid-cols-5">{["Checkout","Restore + build","Test + analyze","Publish artifact","Deploy"].map((x,i)=><div key={x} className="rounded-xl border border-[var(--hairline)] bg-[var(--bg)] p-4"><span className="font-mono text-xs text-[var(--accent)]">0{i+1}</span><p className="mt-3 text-xs font-semibold">{x}</p></div>)}<div className="sm:col-span-5"><Explain title="Configuration matters">Environment-specific endpoints, credentials, feature flags, and secrets belong in configuration/secret stores rather than source. The static demo deliberately uses demo data because GitHub Pages cannot host the ASP.NET Core server.</Explain></div></div></div></Section>

<Section id="s12" n="12" title="Beyond the web app"><div className="mt-7"><h2 className="max-w-3xl text-3xl font-semibold sm:text-5xl">Extend capabilities without rewriting the core.</h2><p className="mt-5 max-w-3xl leading-7 text-[var(--muted)]">These technologies are architectural extensions, not claims about what the demo already implements.</p><div className="mt-8 grid gap-4 lg:grid-cols-3">{[[Network,"API gateway","A front door for multiple services: routing, auth enforcement, rate limiting, policy, observability. It should not become a dumping ground for business logic."],[Code2,"Web Components","Framework-neutral custom elements can expose reusable UI across heterogeneous frontends. Interop, event contracts, styling boundaries, and accessibility still need deliberate design."],[Smartphone,".NET MAUI","A native/mobile client could reuse domain/service concepts and call the same APIs while adapting interaction, offline behavior, device capabilities, and lifecycle for mobile."]].map(([I,h,t])=>{const Icon=I as typeof Network;return <article key={String(h)} className="rounded-2xl border border-[var(--hairline)] p-6"><Icon className="text-[var(--accent)]"/><h3 className="mt-6 text-xl font-semibold">{String(h)}</h3><p className="mt-3 text-sm leading-7 text-[var(--muted)]">{String(t)}</p></article>})}</div></div></Section>

<Section id="s13" n="13" title="Engineering decisions + tradeoffs" tint><div className="mt-7"><h2 className="max-w-4xl text-3xl font-semibold sm:text-5xl">The decision is more important than the pattern name.</h2><div className="mt-8 grid gap-px overflow-hidden rounded-2xl border border-[var(--hairline)] bg-[var(--hairline)] lg:grid-cols-3">{[["Keep","Strong typing, explicit component ownership, async I/O, narrow service boundaries, accessible states."],["Re-evaluate at scale","Repository abstraction over EF Core, client state strategy, caching, API shape, component granularity."],["Add for production","Persistent DB, auth/RBAC, validation, resilience, observability, comprehensive tests, deployment environments."]].map(([h,t])=><div key={h} className="bg-[var(--bg)] p-6"><p className="text-xs font-semibold text-[var(--accent)]">{h}</p><p className="mt-4 text-sm leading-7 text-[var(--muted)]">{t}</p></div>)}</div><div className="mt-5"><Talk>A senior discussion should include what I would change as requirements change. Architecture is not choosing the maximum number of patterns; it is choosing boundaries and tradeoffs appropriate to the system’s current risks.</Talk></div></div></Section>

<Section id="s14" n="14" title="Interview map"><div className="mt-7 grid gap-10 lg:grid-cols-[.7fr_1.3fr]"><div><h2 className="text-3xl font-semibold sm:text-5xl">The concepts I should be ready to explain.</h2><p className="mt-5 leading-7 text-[var(--muted)]">Use this as the final review before a technical conversation. Each topic connects back to a concrete part of the walkthrough rather than a memorized definition.</p><a className="btn-solid mt-7" href="https://github.com/dgutensohn03/dotnet-insurance-dashboard" target="_blank" rel="noreferrer"><Github size={16}/>Open source alongside this page</a></div><div className="grid gap-3 sm:grid-cols-2">{[["C# / .NET","typing · records/classes · nullability · decimal · collections · async/await · Task<T>"],["LINQ","Where · Select · Any/All · FirstOrDefault · ordering · aggregation · deferred execution"],["Blazor","Razor · routing · state · parameters · EventCallback · binding · lifecycle · forms"],["ASP.NET Core","routing · model binding · middleware · JSON · CORS · HTTP status codes · validation"],["DI","contracts · implementations · composition root · transient/scoped/singleton · testability"],["Data","repository tradeoffs · EF Core · SQL · DTOs · transactions · query performance"],["Security","authentication · authorization · RBAC/policies · tokens · secrets · server enforcement"],["Quality","unit/component/integration/E2E · accessibility · errors · logging · metrics · tracing"],["Architecture","boundaries · coupling · cohesion · API gateway · Web Components · MAUI"],["Delivery","GitHub Actions · artifacts · configuration · environments · rollback · observability"]].map(([h,t])=><div key={h} className="rounded-xl border border-[var(--hairline)] p-4"><p className="text-sm font-semibold">{h}</p><p className="mt-2 text-xs leading-5 text-[var(--muted)]">{t}</p></div>)}</div></div><div className="mt-10 rounded-2xl border border-[var(--accent)]/25 bg-[var(--accent)]/[.05] p-6 sm:p-8"><div className="flex items-center gap-3"><BookOpen className="text-[var(--accent)]"/><h3 className="text-xl font-semibold">A strong five-minute walkthrough</h3></div><p className="mt-4 max-w-4xl text-sm leading-7 text-[var(--muted)]">Start with the business problem → show the four system boundaries → follow CLM-10482 from repository to screen → explain Blazor state and component communication → show the API and DI registration → explain why the public demo swaps its data implementation → finish with what you would add or reconsider in production.</p></div></div></Section>

<section className="border-t border-[var(--hairline)] px-5 py-20 text-center sm:px-6 lg:py-28"><div className="mx-auto max-w-4xl"><p className="text-xs font-bold uppercase tracking-[.25em] text-[var(--accent)]">DG · DO GOOD</p><h2 className="mt-6 text-3xl font-semibold sm:text-5xl">Build the system. Understand the decisions.</h2><p className="mx-auto mt-5 max-w-2xl leading-7 text-[var(--muted)]">The goal is not to memorize framework vocabulary. It is to understand what each part contributes, how the pieces interact, where they can fail, and why the architecture changes as the product grows.</p><div className="mt-8 flex flex-wrap justify-center gap-3"><a className="btn-solid" href="https://dgutensohn03.github.io/dotnet-insurance-dashboard/" target="_blank" rel="noreferrer">Open dashboard<ExternalLink size={15}/></a><Link href="/" className="btn"><ArrowLeft size={16}/>Return to portfolio</Link></div></div></section>
</main></div>}
